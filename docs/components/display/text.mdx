---
sidebar_position: 1
hide_title: true
title: Component | Text
description: Render text.
---

import Tabs from "@theme/Tabs";
import TabItem from "@theme/TabItem";
import IFrame from "../../../src/iframe";
import { Parameter, ParameterTemplate } from "../../../src/components" 


# Component | Text

Render text

<Tabs groupId="sdkLanguage">
    <TabItem value="node" label="TypeScript / JavaScript">
    ```js
    ui.text("Hello world!")
    ```
    </TabItem>
    <TabItem value="python" label="Python">
    ```python
    ui.text("Hello world!")
    ```
    </TabItem>
</Tabs>

<IFrame type="components-display-text" heightPx={150} />

## Color & Size

Use the `color` and `size` properties to quickly change the appearance of the text. See the API reference below for the full list of supported values for each.

<Tabs groupId="sdkLanguage">
    <TabItem value="node" label="TypeScript / JavaScript">
    ```js
    page.add(() => ui.stack(
        [
            ui.text("Users", { color: "primary", size: "xl" }),
            ui.text("10 users found in database.", { color: "text-secondary" })
        ],
        { spacing: "4px" }
    ))
    ```
    </TabItem>
    <TabItem value="python" label="Python">
    ```python
    page.add(lambda: ui.stack(
        [
            ui.text("Users", color="primary", size="xl"),
            ui.text("10 users found in database.", color="text-secondary")
        ],
        spacing="4px"
    ))
    ```
    </TabItem>
</Tabs>

<IFrame type="components-display-text-appearance" heightPx={150} />

## Nested Text

You can nest text components to create more complex text structures.

<Tabs groupId="sdkLanguage">
    <TabItem value="node" label="TypeScript / JavaScript">
    ```js
    page.add(() => ui.text([
        "It is ",
        ui.text("very important", { color: "warning", style: { fontWeight: "600" }}),
        " very important that you know this..."
    ]))
    ```
    </TabItem>
    <TabItem value="python" label="Python">
    ```python
    page.add(lambda: ui.text([
        "It is ",
        ui.text("very important", color="warning", style={"fontWeight": "600"}),
        " very important that you know this..."
    ]))
    ```
    </TabItem>
</Tabs>

<IFrame type="components-display-text-nested" heightPx={150} />

## API reference

### Function signature

<Tabs groupId="sdkLanguage">
    <TabItem value="node" label="TypeScript / JavaScript">
    ```js
    ui.text(
        text: string | TextComponent | Array<string | TextComponent>,
        properties?: Partial<{
            color: "text" | "text-secondary" | "primary" | "success" | "warning" | "danger" | "background",
            size: "xs" | "sm" | "md" | "lg" | "xl",
            style: Style
        }>
    )
    ```
    </TabItem>
    <TabItem value="python" label="Python">
    ```python
    ui.text(
        text: str | TextComponent | list[str | TextComponent],
        *,
        color: Literal["text", "text-secondary", "primary", "success", "warning", "danger", "background"] = "text",
        size: Literal["xs", "sm", "md", "lg", "xl"] = "md",
        style: Style | None = None
    )
    ```
    </TabItem>
</Tabs>

### Parameters

<Parameter
    name="text"
    required={true}
    type={lang => lang === "js" ? "string | TextComponent | Array\<string | TextComponent>" : "string | TextComponent | list[string | TextComponent]"}
    description="The text to be displayed."
/>

<Parameter
    name="color"
    required={false}
    type="literal"
    description={
        <>
            <p>
              Specifies the color of the text. Defaults to <code>"text"</code>
            </p>
            <p>Options:</p>
            <ul>
              <li>
                <code>"text"</code>: Default text color
              </li>
              <li>
                <code>"text-secondary"</code>: Secondary text color, uses a lighter shade of the default text color
              </li>
              <li>
                <code>"primary"</code>: Primary color. Defaults to a shade of blue. Can be set via the <a href="/guides/styling-your-app#theming">theme</a>.
              </li>
              <li>
                <code>"success"</code>: Success color, green.
              </li>
              <li>
                <code>"warning"</code>: Warning color, orange.
              </li>
              <li>
                <code>"danger"</code>: Danger color, red.
              </li>
              <li>
                <code>"background"</code>: Background color. Can be used over inverted backgrounds.
              </li>
            </ul>
        </>
    }
    version="0.19.1"
/>

<Parameter
    name="size"
    required={false}
    type="literal"
    description={
        <>
            <p>
              Specifies the size of the text. Defaults to <code>"md"</code>
            </p>
            <p>Options:</p>
            <ul>
              <li>
                <code>"xs"</code>: Extra small text
              </li>
              <li>
                <code>"sm"</code>: Small text
              </li>
              <li>
                <code>"md"</code>: Medium text
              </li>
              <li>
                <code>"lg"</code>: Large text
              </li>
              <li>
                <code>"xl"</code>: Extra large text
              </li>
            </ul>
        </>
    }
    version="0.19.1"
/>

<ParameterTemplate.Style />