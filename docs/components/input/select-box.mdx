---
sidebar_position: 1
hide_title: true
title: Component | Select
description: Render a select dropdown.
---

import Tabs from "@theme/Tabs";
import TabItem from "@theme/TabItem";
import IFrame from "../../../src/iframe";
import { ParameterTemplate } from "../../../src/components"


# Component | Select

Render a select dropdown.

<Tabs groupId="sdkLanguage">
    <TabItem value="node" label="TypeScript / JavaScript">
    ```js
    ui.selectBox("select-input-key", ["A", "B", "C", "D"], { 
        label: "Select an option",
        onChange: (value) => console.log(value)
    })
    ```
    </TabItem>
    <TabItem value="python" label="Python">
    ```python
    ui.select_box(
        "text-input-key",
        ["A", "B", "C", "D"],
        label="Select an option",
        on_change=lambda value: print(value)
    )
    ```
    </TabItem>
</Tabs>

<IFrame type="components-input-select" heightPx={325} />

Learn more about [working with inputs](/guides/forms).

## Options

Select components support both a "basic" and "detailed" format for options.

<Tabs groupId="sdkLanguage">
  <TabItem value="node" label="TypeScript / JavaScript"></TabItem>
  <TabItem value="python" label="Python"></TabItem>
</Tabs>

### Basic

Quick and easy. Pass an array of strings, such as `["A", "B", "C", "D"]`.

<Tabs groupId="sdkLanguage" className="hide_tabs">
    <TabItem value="node" label="TypeScript / JavaScript">
    ```js
    ui.selectBox("select-input-key", ["A", "B", "C", "D"], { 
        label: "Select an option",
        onChange: (value) => console.log(value)
    })
    ```
    </TabItem>
    <TabItem value="python" label="Python">
    ```python
    ui.select_box(
        "select-input-key",
        ["A", "B", "C", "D"],
        label="Select an option",
        on_change=lambda value: print(value)
    )
    ```
    </TabItem>
</Tabs>

### Detailed

<Tabs groupId="sdkLanguage" className="hide_tabs">
    <TabItem value="node" label="TypeScript / JavaScript">
    Use separate `label` and `value` fields for each option. The `label` field will be displayed to the user, and the `value` field will be passed to hooks such as `onChange` and `validate`.

    ```js
    const options = [
        { label: "Google", value: "GOOG" },
        { label: "Apple", value: "APPL" },
        {
            label: "Microsoft",
            value: "MSFT",
            description: "Current stock price: $423"
        }
    ]

    ui.selectBox("company-select", options, {
        label: "Select a company",
        onChange: (value) => console.log(value) // Will be "GOOG", "APPL", or "MSFT"
    })
    ```
    </TabItem>
    <TabItem value="python" label="Python">
    Use separate `label` and `value` fields for each option. The `label` field will be displayed to the user, and the `value` field will be passed to hooks such as `on_change` and `validate`.

    ```python
    options = [
        { "label": "Google", "value": "GOOG" },
        { "label": "Apple", "value": "APPL" },
        {
            "label": "Microsoft",
            "value": "MSFT",
            "description": "Current stock price: $423"
        }
    ]

    ui.select_box(
        "company-select",
        options,
        label="Select an option",
        on_change=lambda value: print(value) # Will be "GOOG", "APPL", or "MSFT"
    )
    ```
    </TabItem>

</Tabs>

### Detailed option object properties

<ParameterTemplate.SelectOptionsObject />

## API reference

### Function signature

<Tabs groupId="sdkLanguage">
    <TabItem value="node" label="TypeScript / JavaScript">
    ```js
    type OptionValue = string | number | boolean

    type DetailedOption = {
        label: string,
        value: OptionValue,
        description?: string
    }

    ui.selectBox(
        id: string,
        options: Array<OptionValue | DetailedOption>
        properties: Partial<{
            label: string,
            description: string,
            required: boolean,
            initialValue: OptionValue | null,
            validate: (value: OptionValue | null) => string | void,
            onChange: (value: OptionValue | null) => void,
            style: Style
        }>
    )
    ```
    </TabItem>
    <TabItem value="python" label="Python">
    ```python
    OptionValue = str | float | int | bool

    class DetailedOption(TypedDict):
        label: str
        value: OptionValue
        description: NotRequired[str]

    ui.select_box(
        id: str,
        options: list[OptionValue | DetailedOption]
        *,
        label: str | None = None,
        description: str | None = None,
        required: bool = True,
        initial_value: OptionValue | None = None,
        validate: Callable[[OptionValue | None], str | None] = None,
        on_change: Callable[[OptionValue | None], None] = None,
        style: Style | None = None
    )
    ```
    </TabItem>

</Tabs>

### Parameters

<ParameterTemplate.Id />

<ParameterTemplate.SelectOptions optionsSectionLink="#options" />

<ParameterTemplate.Label
    componentName="select box"
/>

<ParameterTemplate.Description
    componentName="select box"
/>

<ParameterTemplate.Required
    parameter="required"
    componentName="select box"
    hookName={lang => lang === "js" ? "onChange" : "on_change"}
/>

<ParameterTemplate.InitialValue
    componentName="select box"
    type={lang => lang === "js" ? "string | number | boolean | null" : "str | float | int | bool | None"}
/>

<ParameterTemplate.ValidateInput
    componentName="select"
    hookInputArgument={lang => lang === "js" ? "value: string | number | boolean | null" : "str | float | int | bool | None"}
/>

<ParameterTemplate.OnChange
    componentName="select"
    hookInputArgument={lang => lang === "js" ? "value: string | number | boolean | null" : "str | float | int | bool | None"}
/>

<ParameterTemplate.Style />