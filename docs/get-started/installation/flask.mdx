---
sidebar_position: 2
hide_title: true
title: Installation | Flask
description: Install the Compose SDK into your Flask project.
---

import Tabs from "@theme/Tabs";
import TabItem from "@theme/TabItem";
import IFrame from "../../../src/iframe";
import { Introduction, InstallSdkPython, ReplaceApiKey, NextStepsPython, StarterCodePython, ConnectClientDetailsPython } from "../../../src/components/InstallationSteps";

# Installation | Flask

<Introduction framework="Flask" newProjectLink="/get-started/installation/new-project/python" language="Python" />

## 1. Install the SDK

<InstallSdkPython />

## 2. Add starter apps

Create a new file in your project where you'll add your Compose apps, e.g. `compose.py`, and paste the following code:

<StarterCodePython />

<ReplaceApiKey />

## 3. Start the Compose Client

In the main entry point of your project, e.g. `main.py`, add the following:

```python
from flask import Flask, jsonify
from .compose import compose_client
import os

# Avoid running the client in the auto-reload process
if os.environ.get("WERKZEUG_RUN_MAIN") == "true":
    compose_client.connect_async()

app = Flask(__name__)
```

<ConnectClientDetailsPython framework="Flask" />

## 4. Run your app

Start your Flask server. Compose will automatically connect in the background.

```bash
flask --app main run
```

You should see your apps come online in the [Compose web dashboard](https://app.composehq.com/home).

## Next steps

<NextStepsPython />