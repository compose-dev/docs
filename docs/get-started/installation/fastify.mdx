---
sidebar_position: 2
hide_title: true
title: Installation | Fastify
description: Install the Compose SDK into your Fastify project.
---

import Tabs from "@theme/Tabs";
import TabItem from "@theme/TabItem";
import IFrame from "../../../src/iframe";
import { Introduction, InstallSdkNode, AddStarterAppsNode, RunAppNode, NextStepsNode } from "../../../src/components/InstallationSteps";

# Installation | Fastify

<Introduction framework="Fastify" newProjectLink="/get-started/installation/new-project/node-js" language="Node.js" />

## 1. Install the SDK

<InstallSdkNode />

## 2. Add starter apps

<AddStarterAppsNode />

## 3. Start the Compose Client

In the main entry point of your project, e.g. `index.ts`, import the Compose Client that you exported in the previous step and initialize it:

```ts
// highlight-start
import { composeClient } from "./compose";
// highlight-end

// ...

fastify.listen({ port: 3000 }, (err, address) => {
  if (err) {
    fastify.log.error(err);
    process.exit(1);
  }
  fastify.log.info(`server listening on ${address}`);
  // highlight-start
  composeClient.connect();
  // highlight-end
});
```

## 4. Run your app

<RunAppNode />

## Next steps

<NextStepsNode />