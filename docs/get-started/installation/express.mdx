---
sidebar_position: 2
hide_title: true
title: Installation | Express
description: Install the Compose SDK into your Express project.
---

import Tabs from "@theme/Tabs";
import TabItem from "@theme/TabItem";
import IFrame from "../../../src/iframe";
import { Introduction, InstallSdkNode, AddStarterAppsNode, RunAppNode, NextStepsNode } from "../../../src/components/InstallationSteps";

# Installation | Express

<Introduction framework="Express" />

## 1. Install the SDK

<InstallSdkNode />

## 2. Add starter apps

<AddStarterAppsNode />

## 3. Start the Compose Client

In the main entry point of your project, e.g. `index.ts`, import the Compose Client that you exported in the previous step and initialize it:

```ts
// highlight-start
import { composeClient } from "./compose";
// highlight-end

// ...

app.listen(3000, () => {
    console.log("Server is running on port 3000");
    // highlight-start
    composeClient.connect();
    // highlight-end
});
```

## 4. Run your app

<RunAppNode />

## Next steps

<NextStepsNode />